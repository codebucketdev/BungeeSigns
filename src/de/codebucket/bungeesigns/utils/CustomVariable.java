package de.codebucket.bungeesigns.utils;

public class CustomVariable 
{
	private String rule;
	
	public CustomVariable(String rule)
	{
		this.rule = rule;
	}
	
	public String parseVariable(String input)
	{
		if(rule.startsWith("split("))
		{
			
		}
		else if(rule.startsWith("substring("))
		{
			
		}
		else if(rule.startsWith("replace("));
		{
			
		}
		
		return null;
	}
}
